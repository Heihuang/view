1、RB树是二叉搜索树
2、每个节点不是红色就是黑色
3、根节点为黑色
4、如果节点为红，其子节点必须为黑
5、任一节点至NULL的任何路径，所含的黑节点数必须相同
6、新增节点必须为红，新增节点的父节点必须为黑
strcut rb_tree_node{
    bool color; //节点颜色，非红即黑
    rb_tree_node *parent; //父节点
    rb_tree_node* left; //左节点
    rb_tree_node* right; //右节点
}
